Finance reports a problem with the bundled opsdesk project: the daily totals of two neighbouring days
sometimes add up to more than the total of both days together. It looks like a transaction booked
exactly at midnight is counted on both days, both in `python -m opsdesk nightly <YYYY-MM-DD>` and when
the aggregation is used directly.

No file or function is known to be involved. Diagnose the root cause from the symptom and the sample
data, then fix it with the smallest change. Do not modify the existing tests or the files in `data/`.

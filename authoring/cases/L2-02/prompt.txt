Introduce a `CANCELLED` order status in the bundled order mini-project:

- orders that are `NEW` or `PAID` can be cancelled; `SHIPPED` and `FINISHED` orders cannot;
- `CANCELLED` is terminal: no transition leaves it;
- the order summary renders the new status as `Cancelled`.

Every existing transition and label must keep behaving exactly as today. Do not modify the existing
tests.

`is_leap_year` in `dates.py` treats every year divisible by 4 as a leap year, so century years such
as 1900 wrongly get a 29-day February. The included failing unit test captures the Gregorian rule.

Fix `dates.py` with a minimal change and make the whole test suite pass. Change no other source file
and do not modify the existing tests. Acceptance also runs hidden century-rule and month-length cases.

A previous attempt left an unfinished fix in `slugify.py`, and the test suite is currently red. Read
the failing output, finish the implementation and iterate until the whole suite is green. Expect more
than one round: the first error you see is not the only problem.

Change only `slugify.py` and do not modify the existing tests. Acceptance also checks hidden inputs.

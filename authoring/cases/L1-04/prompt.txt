`conversion_rate(clicks, visits)` in `metrics.py` uses integer division, so 2 conversions out of 3
visits are reported as 0%. It must return the ratio as a float rounded to two decimals (2 of 3 gives
0.67). The report renderer in `report.py` formats that ratio and must keep working unchanged.

Fix `metrics.py` with a minimal change and make the whole test suite pass. Change no other source file
and do not modify the existing tests. Acceptance also runs hidden rounding cases.

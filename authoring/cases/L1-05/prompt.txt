The product spec requires record search to ignore the letter case of ASCII input, but
`matches(query, records)` in `search.py` compares records case-sensitively. Failing unit tests show
the expected behaviour.

Fix the comparison in `search.py` with a minimal change and make the whole test suite pass. Change no
other source file and do not modify the existing tests. Acceptance also runs hidden queries.

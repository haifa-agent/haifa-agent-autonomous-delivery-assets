`PageWindow.slice(items, page, size)` in `pagination.py` returns the wrong window when the requested
page is only partially filled: instead of the remaining items it returns a full-size window that
repeats items from the previous page. A failing unit test in the workspace reproduces the problem.

Fix the bug with a minimal change to `pagination.py` and make the whole test suite pass. Change no
other source file and do not modify the existing tests. Acceptance also runs hidden boundary cases.

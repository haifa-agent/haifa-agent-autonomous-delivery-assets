Dashboard users of the bundled mini-project want to choose how many records a listing returns.

Add an optional `page_size` argument to `list_records`: any integer from 1 to 100 (inclusive) is
accepted, anything else raises `ValueError`, and the default stays 20. Existing callers of
`list_records` must keep working.

Do not modify the existing tests.

The nightly report job of the bundled opsdesk project crashed in production; the job log is stored
under `logs/`.

Someone already attempted a fix and it is in the working tree: the job no longer crashes, but the test
suite is still red and operators say that accounts without activity have vanished from the report.
Every account must appear in the nightly report, with the usual `(no rows)` placeholder when it had
no activity that day.

Find the actual root cause, correct or undo whatever in the earlier attempt is wrong, and iterate
until the whole suite is green. Do not modify the existing tests or the files in `data/` and `logs/`.

Support ticket for the bundled opsdesk project:

> On Windows, `python -m opsdesk settings C:\Users\dev\opsdesk` prints the built-in defaults for
> `cache_dir` and `report_dir` instead of the values from `data/settings.ini`. A colleague whose project
> lives elsewhere on Windows gets paths with strange whitespace in them instead. The very same settings
> file works fine on Linux and macOS.

No file or function is known to be involved. Find the root cause and fix it with a minimal change. Do
not modify the existing tests or the files in `data/`.

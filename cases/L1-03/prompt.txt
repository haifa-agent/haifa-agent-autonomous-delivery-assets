The workspace contains `slugify.py` and a unit test suite that is currently red on hyphen edge
cases. A previous fix attempt is already in the working tree and is still red: read the failing
output, correct the implementation, and iterate until the suite is green.

The first attempt is not expected to be the final one. Change only `slugify.py`; do not modify
the existing tests.